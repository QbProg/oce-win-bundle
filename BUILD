To Build oce-win-bundle:

- run cmake-gui 
- Set the "where is the source code" field to the sources directory
- Set the "where to build binaris" to your preffered build dir (different from the source one)
- Configure
- Set the INSTALL directory to a place where you want to put the oce-win-bundle 
resulting binaries
- Configure
- Generate
- Compile in DEBUG and RELWITHDEBINFO
- INSTALL both DEBUG and RELWITHDEBINFO

- Done

If you want to build win32 and win64, for the moment please select two different
INSTALL directories.
 